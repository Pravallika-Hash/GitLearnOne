name: My First workflow

run-name: ${{ github.actor }} is learning Github Actions


on: 
 push:
 workflow_dispatch:

jobs:
 echo-print:
  runs-on: ubuntu-latest
  steps: 
   - run: echo "Run the first workflow"
 
